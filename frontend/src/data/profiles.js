// Shared status label map — used by the real roster/table now that both
// fetch profiles from the API (see lib/api.js), not by mock data anymore.
export const statusLabel = {
  complete: 'Complete',
  draft: 'Draft',
  in_review: 'In review',
}
